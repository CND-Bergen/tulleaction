name: "Totally bogus action"
description: "Used for demonstration purposes"
runs:
  using: composite
  steps:
    - name: Important stuff
      id: important_stuff
      shell: bash
      run: |
        echo "This not the same"
